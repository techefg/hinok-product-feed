{
  "name": "hinok-product-feed",
  "version": "1.0.0",
  "description": "Direct Meta and Google product feed generator for Hinok US Shopify store",
  "type": "module",
  "scripts": {
    "generate": "node src/generate-feed.js",
    "generate:dry": "node src/generate-feed.js --dry-run",
    "google:merchant": "node src/google-merchant.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true
}
